'use client';
import { useEffect, useState } from 'react';
import { usePathname, useRouter } from 'next/navigation';
const items = [{ href: '/', label: 'نمای کلی', key: 'ن' }, { href: '/trips', label: 'سفرها', key: 'س' }, { href: '/plates', label: 'پلاک‌ها', key: 'پ' }, { href: '/settings', label: 'تنظیمات', key: 'ت' }];
export default function AppNav() { const pathname = usePathname(); const router = useRouter(); const [open, setOpen] = useState(false); const [palette, setPalette] = useState(false); useEffect(() => { const onKey = (e: KeyboardEvent) => { if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') { e.preventDefault(); setPalette(true); } if (e.key === 'Escape') { setPalette(false); setOpen(false); } }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []); const go = (href: string) => { router.push(href); setOpen(false); setPalette(false); }; return <><div className="nav-links" role="navigation" aria-label="ناوبری اصلی">{items.map(i => <button key={i.href} className={pathname === i.href ? 'active' : ''} onClick={() => go(i.href)}>{i.label}</button>)}</div><div className="nav-actions"><button className="search-trigger" onClick={() => setPalette(true)} aria-label="باز کردن منوی فرمان"><span>⌕</span><kbd>⌘ K</kbd></button><button className="mobile-menu-button" onClick={() => setOpen(!open)} aria-label="باز و بسته کردن منو">{open ? '×' : '☰'}</button><div className="nav-avatar">A</div></div>{open && <div className="mobile-nav">{items.map(i => <button key={i.href} className={pathname === i.href ? 'active' : ''} onClick={() => go(i.href)}>{i.label}<span>{i.key}</span></button>)}</div>}{palette && <div className="palette-backdrop" onMouseDown={() => setPalette(false)}><div className="command-palette" role="dialog" aria-modal="true" aria-label="منوی فرمان" onMouseDown={e => e.stopPropagation()}><div className="palette-input"><span>⌕</span><input autoFocus placeholder="جست‌وجوی صفحه یا عملیات…" onChange={e => { const q = e.target.value.toLowerCase(); document.querySelectorAll<HTMLButtonElement>('.command-item').forEach(el => { el.hidden = !el.innerText.toLowerCase().includes(q); }); }} /><kbd>ESC</kbd></div><div className="command-list"><p>رفتن به</p>{items.map(i => <button className="command-item" key={i.href} onClick={() => go(i.href)}><span className="command-icon">{i.key}</span>{i.label}<kbd>↵</kbd></button>)}<p>عملیات سریع</p><button className="command-item" onClick={() => go('/settings')}><span className="command-icon">+</span>اتصال اپ موبایل<kbd>↵</kbd></button></div></div></div>}</>; }
